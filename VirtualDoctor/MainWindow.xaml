<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local="clr-namespace:VirtualDoctor" mc:Ignorable="d" x:Class="VirtualDoctor.MainWindow"
        Title="Virtual Doctor - Administration Panel" Height="565.589" Width="1024.049" Loaded="Window_Loaded">
    <Window.Resources>
        <CollectionViewSource x:Key="symptomCathegoryViewSource" d:DesignSource="{d:DesignInstance {x:Type local:SymptomCathegory}, CreateList=True}"/>
    </Window.Resources>
    <Grid>
        <Label x:Name="Label1" Content="" HorizontalAlignment="Left" Margin="106,46,0,0" VerticalAlignment="Top" Width="132"/>
        <TabControl HorizontalAlignment="Left" Height="486" Margin="25,29,0,0" VerticalAlignment="Top" Width="727">
            <TabItem Header="Symptom cathegories">
                <Grid Background="#FFE5E5E5" Margin="2,0,-2,0" DataContext="{StaticResource symptomCathegoryViewSource}" >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Button Name="Button1" Content="Button" HorizontalAlignment="Left" Margin="451,49,0,0" VerticalAlignment="Top" Width="75" Click="Button1_Click"/>
                    <DataGrid HorizontalAlignment="Left" Margin="28,60,0,0" VerticalAlignment="Top" Height="218" Width="368" ItemsSource="{Binding}" AutoGenerateColumns="False" EnableRowVirtualization="True" >
                        <DataGrid.Columns>
                            <DataGridTextColumn  x:Name="CategoryIdColumn" Binding="{Binding Id}"  
                                Header="Id" Width="SizeToHeader" IsReadOnly="True"/>
                            <DataGridTextColumn x:Name="NameColumn" Binding="{Binding Name}"  
                                Header="Name:" Width="SizeToHeader"/>
                            <DataGridComboBoxColumn x:Name="CathegoryParentColumn" SelectedValueBinding="{Binding Name, Mode=TwoWay}" DisplayMemberPath="{Binding Name}"  
                                Header="Cathegory parent:" Width="SizeToHeader"/>
                        </DataGrid.Columns>
                    </DataGrid>

                </Grid>
            </TabItem>
            <TabItem Header="Symptoms">
                <Grid Background="#FFE5E5E5"/>
            </TabItem>
            <TabItem Header="Diseases" HorizontalAlignment="Left" Height="20" VerticalAlignment="Top">
                <Grid Background="#FFE5E5E5"/>
            </TabItem>
            <TabItem Header="Disease corelation" HorizontalAlignment="Left" Height="20" VerticalAlignment="Top">
                <Grid Background="#FFE5E5E5"/>
            </TabItem>
            <TabItem Header="Symptom - disease connection" HorizontalAlignment="Left" Height="20" VerticalAlignment="Top">
                <Grid Background="#FFE5E5E5"/>
            </TabItem>
        </TabControl>

    </Grid>
</Window>
